import { HttpClient } from '@angular/common/http';
import { Injectable } from '@angular/core';

@Injectable({
  providedIn: 'root',
})
export class CivicEventsService {
  constructor(private http: HttpClient) {}

  // getFarmersMarkets(): Observable<
}
